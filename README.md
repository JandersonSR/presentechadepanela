# Calendario-editavel

### Para iniciar o projeto
> python -m venv .venv
> source .venv/bin/activate ou source .venv/Scripts/activate
> pip install -r requirements.txt


## rodar normalmente
streamlit run app.py ou python streamlit run app.py

## rodar o projeto em porta específica
python streamlit run app.py --server.port 8502

## presenteschadepanela -> https://presentechadepanela.streamlit.app/
